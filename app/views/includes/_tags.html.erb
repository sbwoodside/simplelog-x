<div>
  <ul class="inline">
    <% mytags.sort.each do |tag| %>
      <li><%= link_to tag, "/#{Site.archive_token}/tags/#{tag}" , :title => "View posts tagged with &quot;#{tag}&quot;" %></li>
    <% end %>
  </ul>
</div>